- @articles = Article.order(:published => :desc)

h1 Lista de Artigos

table.table.tabls-striped
  thead
    tr
      th Título
      th Publicado
      th Categoria
      th       

  tbody
    - @articles.each do |article|
      tr
        td = article.title
        td = l article.published, format: :short
        td = article.category.name
        td
          .btn-group.pull-right
            = link_to 'Exibir', article, class: "btn btn-default"
            = link_to 'Editar', edit_article_path(article), class: "btn btn-editar"
            = link_to 'Excluir', article, data: { confirm: 'Tem certeza?' }, method: :delete, class: "btn btn-excluir"

br

= link_to 'Novo Artigo', new_article_path, class: "btn btn-primary"
